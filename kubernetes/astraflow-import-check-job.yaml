apiVersion: batch/v1
kind: Job
metadata:
  name: astraflow-import-check
  namespace: astraflow
  labels:
    app.kubernetes.io/name: astraflow
    app.kubernetes.io/component: astraflow
    app.kubernetes.io/managed-by: autopoc
    scenario: import-check
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 60
  template:
    metadata:
      labels:
        app.kubernetes.io/name: astraflow
        app.kubernetes.io/component: astraflow
        scenario: import-check
    spec:
      containers:
        - name: astraflow
          image: quay.io/aicatalyst/astraflow-astraflow:latest
          imagePullPolicy: Always
          command: ["python"]
          args: ["-c", "import astraflow; print('version:', astraflow.__version__)"]
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      restartPolicy: Never
